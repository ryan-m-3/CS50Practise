#include <cs50.h>
#include <stdio.h>

int main (void)
{
    //for loop to call your custom function
    for (int i = 0; i < 3; i++)
    {
        meow();
    }
}


//the following code has just created our own function called meow. 
void meow(void)
{
    printf("meow\n");
}
